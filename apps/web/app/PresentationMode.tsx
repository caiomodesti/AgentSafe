"use client";
import { useState } from "react";
const STEPS = [{ title: "A pergunta que importa", cue: "Quanto esse agente pode perder agora?", detail: "O AgentSafe começa pela autoridade financeira real, não por uma tela de limites configurados manualmente." }, { title: "Descoberta automática", cue: "O sistema encontra oito maneiras de esse agente falhar.", detail: "Orçamento ausente, loop de tentativas pagas, merchant desconhecido e novo destinatário são exemplos detectados." }, { title: "Policy explicável", cue: "Os riscos viram limites de US$ 2, US$ 3 e US$ 20.", detail: "Cada limite é explicado pelo finding que o originou — e pode ser revisado antes de ativar." }, { title: "Enforcement real", cue: "US$ 0,55 passa. US$ 21 não passa.", detail: "Na Solana Devnet, a autoridade delegada impede a transação acima do teto antes que tokens sejam movimentados." }] as const;
export function PresentationMode() { const [step, setStep] = useState(0); const current = STEPS[step]; return <section className="presentation"><div><p className="eyebrow">ROTEIRO DE APRESENTAÇÃO · {step + 1} DE {STEPS.length}</p><h2>{current.title}</h2><p className="presentation-cue">{current.cue}</p><p className="muted">{current.detail}</p></div><div className="presentation-controls"><div className="dots">{STEPS.map((item, index) => <button key={item.title} className={index === step ? "dot active" : "dot"} onClick={() => setStep(index)} aria-label={item.title} />)}</div><button onClick={() => setStep((step + 1) % STEPS.length)}>Próximo passo →</button></div></section>; }
