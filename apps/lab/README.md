# Agent Financial Lab

Self-contained static interface for the ResearchBot product demo. Open `index.html` in a browser to inspect the current simulated journey.

It deliberately has no runtime dependency on Solverdict, a wallet, or a payment provider. The next iteration will bind its displayed values to the `runResearchBotDemo()` contract and add the explicit Devnet proof state.
